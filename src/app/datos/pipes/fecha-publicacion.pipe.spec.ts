import { FechaPublicacionPipe } from './fecha-publicacion.pipe';

describe('FechaPublicacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaPublicacionPipe();
    expect(pipe).toBeTruthy();
  });
});
